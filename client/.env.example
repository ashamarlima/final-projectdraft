# Backend origin used by the API client and by the Vite dev proxy.
# Leave empty to call the same origin: the Vite dev server then
# proxies /api requests to http://localhost:5000 (see vite.config.js).
# Example: VITE_API_URL=http://localhost:5000
VITE_API_URL=
